package org.caleydo.datadomain.genetic.contextmenu.container;

import org.caleydo.core.view.opengl.util.overlay.contextmenu.AItemContainer;

/**
 * Implementation of AItemContainer for pathway items. By passing a pathway item
 * ID all relevant context menu items are constructed automatically
 * 
 * @author Alexander Lex
 * @author Marc Streit
 */
public class PathwayItemContextMenuItemContainer extends AItemContainer {
	/**
	 * Constructor that takes a pathway item ID and creates all context menu
	 * items that are relevant for genes.
	 * 
	 * @param refSeqInt
	 *            a refSeq int representation
	 */
	public PathwayItemContextMenuItemContainer(int pathwayItemID) {
		super();

		// TODO insert items
	}
}
